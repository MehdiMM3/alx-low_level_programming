#include <stdio.h>

/**
 * main - prints
 * @argc: number
 * @argv: pointer
 * Return: 0-success, non-zero-fail
*/

int main(int argc, *argv[] __attribute__((unused)))
{
	printf("%d\n", argc - 1);
	return (0);
}
